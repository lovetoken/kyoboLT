V0.1.0.0100
===========

1. CAPM 분석을 위한 준비
2. portfolio 관련 복수개의 함수관리를 위해 portfolio.R 로 통합 (추후 S3 도입 준비)

* `efff()` function
  - `rg` 인자를 필수인자로 변경
  - `plotly` 인자삭제 : ggplot 객체를 반환시킬 수 있는 준비가 되었으므로 내부적으로 plotly 를 하는 것은 불필요
  - `plot.only.efff` 인자삭제 : 항시 개별상품들에 대한 정보를 플랏팅 하겠다는 의미
* `mvp()` function
  - `aap()` 로 명칭 변경
  - `efff()` 에 맞춘 통일성 부여
  - Minimum Variance Portfolio 는 관심대상에서 제외

Version 0.19.0
==============

* add `mvp()` function
* apply to Readme.md


Version 0.17.0
==============

* add `assess_returns()` function


Version 0.16.3
==============

* add NEWS
* Bug fixed about 0.16.0 version

